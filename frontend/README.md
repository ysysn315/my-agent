# 智能 OnCall 助手 - 前端

基于 Vue 3 + Vite 构建的现代化前端应用。

## 功能特性

- 💬 **智能对话** - 支持快速模式和流式模式
- 🔧 **故障分析** - AIOps 自动根因分析，实时流式输出
- 📁 **知识库管理** - 上传运维文档，AI 自动学习
- 📊 **系统状态** - 查看各服务健康状态和会话统计

## 快速开始

### 安装依赖

```bash
cd frontend
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

## 项目结构

```
frontend/
├── index.html          # 入口 HTML
├── package.json        # 依赖配置
├── vite.config.js      # Vite 配置
├── public/             # 静态资源
└── src/
    ├── main.js         # 应用入口
    ├── App.vue         # 根组件
    ├── styles/
    │   └── main.css    # 全局样式
    └── views/
        ├── ChatView.vue     # 对话视图
        ├── AIOpsView.vue    # 故障分析视图
        ├── UploadView.vue   # 文件上传视图
        └── StatusView.vue   # 系统状态视图
```

## API 接口

前端通过代理连接后端 API：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/chat` | POST | 普通对话 |
| `/api/chat_stream` | POST | 流式对话 |
| `/api/ai_ops` | POST | 普通故障分析 |
| `/api/ai_ops_stream` | POST | 流式故障分析 |
| `/api/upload` | POST | 上传文档 |
| `/api/chat/sessions` | GET | 获取会话列表 |
| `/api/health` | GET | API 健康检查 |
| `/milvus/health` | GET | Milvus 健康检查 |

## 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **marked** - Markdown 解析器
- **highlight.js** - 代码高亮