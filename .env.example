# DashScope API 配置（必填）
# 获取地址：https://dashscope.console.aliyun.com/apiKey
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# Milvus 配置（可选，默认值已在 docker-compose 中设置）
# MILVUS_HOST=milvus
# MILVUS_PORT=19530

# Redis 配置（可选，默认值已在 docker-compose 中设置）
# REDIS_HOST=redis
# REDIS_PORT=6379

# 应用配置（可选）
# APP_ENV=production